def TREE_SP_SOLVER(csp):
    inputs = csp with components x, d, c

n in x
root = any n in x
X.topologicshort(x , root)
    for j = n in 2
        if n != 0:
            for i = 1 in n:
                assignment[X] = any consistent value from Di
                    if not n=! n:
                        print('imposible')
return assignment